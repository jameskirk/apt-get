package emerge.ebuild;

public enum EbuildKeyword {
    
    DESCRIPTION, HOMEPAGE, LICENSE, KEYWORDS, IUSE, SRC_URI, DEFAULT_PATH, NAME_IN_REGISTRY,
    
    x86, amd64,
    
    silent_installer, installer, no_installer;

}
